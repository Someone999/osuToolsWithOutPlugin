namespace osuTools
{
    using System;
    partial class ORTDP
    {
      
        TimeSpan cur, dur;
      /* public void GetTime()
        {
            double cu =ppinfo.CurrentTime.TotalMilliseconds,du= ppinfo.SongDuration.TotalMilliseconds;
            if (cu>du)
            {
                du = cu;
            }
            cur = TimeSpan.FromMilliseconds(cu);
            dur = TimeSpan.FromMilliseconds(du);
        }*/
    }
}